const obj = {
    js : 'javaScript',
    py : 'Python',
    ruby : 'Ruby',
    swift : 'Swift'
}

for (const key in obj) {
    // console.log(`${key} is the short of ${obj[key]}`)

}


// using 'forIn' in arrays

const arr = ['superman', 'batman', 'spiderman', 'flash']

for (const hero in arr){
    // console.log(arr[hero])
}